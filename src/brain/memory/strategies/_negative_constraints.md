# Negative Constraints

Anti-patterns learned from failures. The planner and VisionLoop inject these
as "things NOT to do" when executing strategies at the matching hierarchy level.

<!-- Entries are appended by the Dreaming Engine v2 -->
